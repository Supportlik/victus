"""Alembic migrations.

``alembic.ini`` lives in this package and ``script_location`` points here, so
``victus migrate`` and ``victus serve --migrate`` work from any working
directory (see ``runner.py``). Run ``alembic`` directly with
``alembic -c src/victus/infrastructure/migrations/alembic.ini …`` when developing.
"""
